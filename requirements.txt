# Windows UI Automation engine dependencies for electron/tools/desktop_automation.py
# Install with:  pip install -r requirements.txt
# (Windows only — pywin32's post-install script runs automatically via pip on Windows)

pywinauto>=0.6.8
uiautomation>=2.0.20
pywin32>=306
psutil>=5.9.0
